node_modules/
dist/
.env
.env.local
.betsy/
betsy.config.yaml
data/
*.log
.DS_Store
.claude/

# GCP service account keys — NEVER commit
betsy-*.json
gcp-sa*.json
*service-account*.json

# Заметки с доступами в корне (txt с ключами/паролями) — НИКОГДА не коммитим.
# Отслеживаемых .txt в репозитории нет, так что правило ничего не скрывает.
/*.txt

# Local helpers and temp brainstorm sessions — never commit
.superpowers/
scripts/__pycache__/
scripts/deploy_*.py

# betsy-app Electron package build artifacts
betsy-app/dist/
betsy-app/release/
betsy-app/node_modules/
betsy-app/test-results/
betsy-app/playwright-report/
